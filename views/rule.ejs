<%- include('partials/header') %>

<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center">
    <h3 class="mb-4">📋 ข้อบังคับสหกรณ์/กลุ่มเกษตรกร</h3>
    <% if(user && user.mClass=='kjs'){ %> 
      <a href="/rule/upload" class="btn btn-success mb-3">
        <i class="bi bi-plus-circle"></i> อัปโหลดข้อบังคับ
      </a>
    <% } %>
  </div>

  <!-- ช่องค้นหา -->
  <form method="get" action="/rule" class="mb-3">
    <div class="input-group">
      <input type="text" name="search" id="searchInput" class="form-control" placeholder="ระบุชื่อสถาบัน" value="<%= search || '' %>">
      <button class="btn btn-outline-secondary" type="submit">ค้นหา</button>
      <button class="btn btn-outline-secondary" type="button" onclick="window.location='/rule'">ล้าง</button>
    </div>
  </form>

  <% 
    // Group by c_name
    const grouped = {};
    alls.forEach(item => {
      const name = item.c_name || 'ไม่ระบุชื่อสถาบัน';
      if (!grouped[name]) grouped[name] = [];
      grouped[name].push(item);
    });

    // Sort groups by max rule_id DESC
    const sortedGroups = Object.entries(grouped).sort((a, b) => {
      const maxIdA = Math.max(...a[1].map(r => r.rule_id));
      const maxIdB = Math.max(...b[1].map(r => r.rule_id));
      return maxIdB - maxIdA;
    });

    sortedGroups.forEach(([cName, rules]) => {
  %>
    <h5 class="mt-4 text-primary"><%= cName %></h5>
    <ul class="list-group mb-3">
      <% rules
        .sort((a, b) => {
          // ข้อบังคับหลักมาก่อน
          if (a.rule_type !== b.rule_type) {
            return a.rule_type === 'ER' ? 1 : -1;
          }
          // ถ้าประเภทเดียวกันให้เรียงตามปี
          return a.rule_year - b.rule_year;
        })
        .forEach(rule => { 
      %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <% if(rule.rule_type === 'ER'){ %>
            แก้ไขเพิ่มเติม ฉบับที่ <%= rule.er_no %> พ.ศ. <%= rule.rule_year %>
          <% } else { %>
            ข้อบังคับหลัก ปี พ.ศ. <%= rule.rule_year %>
          <% } %>
          <div>
            <% if(!user){ %>
              <a href="/auth/login" class="btn btn-sm btn-outline-primary">ดาวน์โหลด</a>
            <% } else { %>
              <a href="/rule/<%= rule.rule_id %>" class="btn btn-sm btn-outline-primary" target="_blank">ดาวน์โหลด</a>
              <% if(user && user.mClass=='admin'){ %>
                <a href="/rule/delete/<%= rule.rule_id %>" class="btn btn-sm btn-outline-danger" onclick="return confirm('Are you sure?')">ลบ</a>
              <% } %>
            <% } %>
          </div>
        </li>
      <% }) %>
    </ul>
  <% }) %>

  <!-- Pagination -->
  <nav class="mt-4">
    <ul class="pagination justify-content-center">
      <% for (let i = 1; i <= totalPages; i++) { %>
        <li class="page-item <%= currentPage === i ? 'active' : '' %>">
          <a class="page-link" href="/rule?page=<%= i %><%= search ? '&search=' + encodeURIComponent(search) : '' %>"><%= i %></a>
        </li>
      <% } %>
    </ul>
  </nav>
</div>



<%- include('partials/footer') %>
