<%- include('./partials/header') %>

<form action="/finance/upload" method="POST" enctype="multipart/form-data">
  <h3 class="mt-4">📤 อัปโหลดรายงานงบการเงิน</h3>
  <div class="mb-3">
    <label for="c_group" class="form-label">เลือกกลุ่มส่งเสริมสหกรณ์</label>
    <select id="c_group" class="form-select" required>
      <option value="">-- กรุณาเลือก --</option>
      <option value="group1">กลุ่มส่งเสริมสหกรณ์ 1</option>
      <option value="group2">กลุ่มส่งเสริมสหกรณ์ 2</option>
      <option value="group3">กลุ่มส่งเสริมสหกรณ์ 3</option>
      <option value="group4">กลุ่มส่งเสริมสหกรณ์ 4</option>
      <option value="group5">กลุ่มส่งเสริมสหกรณ์ 5</option>
    </select>
  </div>
  
  <div class="mb-3">
    <label for="c_code" class="form-label">เลือกสหกรณ์/กลุ่มเกษตรกร</label>
    <select name="c_code" id="c_code" class="form-select" required>
      <option value="">-- กรุณาเลือก --</option>
    </select>
  </div>
  <input type="hidden" name="c_name" id="c_name">
  
  <script>
    const cGroupSelect = document.getElementById('c_group');
    const cCodeSelect = document.getElementById('c_code');
    const cNameInput = document.getElementById('c_name');
  
    cGroupSelect.addEventListener('change', async function () {
      const group = this.value;
      cCodeSelect.innerHTML = '<option value="">กำลังโหลด...</option>';
  
      if (!group) {
        cCodeSelect.innerHTML = '<option value="">-- กรุณาเลือกกลุ่มก่อน --</option>';
        return;
      }
  
      try {
        const response = await fetch(`/finance/coops/${group}`);
        const data = await response.json();
  
        if (Array.isArray(data) && data.length > 0) {
          cCodeSelect.innerHTML = '<option value="">-- กรุณาเลือกสถาบัน --</option>';
          data.forEach(coop => {
            const option = document.createElement('option');
            option.value = coop.c_code;
            option.text = coop.c_name;
            cCodeSelect.appendChild(option);
          });
        } else {
          cCodeSelect.innerHTML = '<option value="">ไม่พบสหกรณ์ในกลุ่มนี้</option>';
        }
      } catch (error) {
        console.error('โหลดข้อมูลสหกรณ์ล้มเหลว:', error);
        cCodeSelect.innerHTML = '<option value="">เกิดข้อผิดพลาด</option>';
      }
    });
  
    cCodeSelect.addEventListener('change', function () {
      const selectedOption = this.options[this.selectedIndex];
      cNameInput.value = selectedOption.text;
    });
  </script>
  






    <div class="mb-3">
      <!-- select now year -->
       <div class="mb-3">
        <label for="" class="form-label">ปีบัญชี</label>
        <select
          class="form-select form-select-lg"
          name="end_year"
          id=""
        >
        <% 
         const nowYear = new Date().getFullYear() 
        %>
          <option value="<%= nowYear + 542 %>" ><%= nowYear + 542 %></option>
          <option value="<%= nowYear + 543 %>" selected><%= nowYear + 543 %></option>
          <option value="<%= nowYear + 544 %>" ><%= nowYear + 544 %></option>
        </select>
       </div>
      </div>
     
       
        
      
    <div class="mb-3">
      <label>เลือกไฟล์ PDF</label>
      <input type="file" name="file" class="form-control" required>
    </div>
    <button type="submit" class="btn btn-success">อัปโหลด</button>
  </form>
  <hr>
  <h5 class="mt-5">📂 ไฟล์ที่อัปโหลดล่าสุด</h5>
<table class="table table-bordered">
  <thead>
    <tr>
      <th>ชื่อสหกรณ์</th>
      <th>ปีบัญชี</th>
      <th>ไฟล์</th>
      <th>อัปโหลดโดย</th>
      <th>เมื่อ</th>
    </tr>
  </thead>
  <tbody>
    <% recentUploads.forEach(file => { %>
      <tr>
        <td><%= file.c_name %></td>
        <td><%= file.end_date + " " + file.end_year %></td>
        <td>
          <a href="/download/<%= file.id %>" target="_blank" class="btn btn-sm btn-outline-primary">
            🔗 ดูไฟล์
          </a>
        </td>
        <td><%= file.saveby %></td>
        <td>
          <%= new Date(file.savedate).toLocaleDateString('th-TH', {
                year: 'numeric',
                month: 'long',
                day: 'numeric'
          }) %>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>
<div class="mt-4 text-end">
    <a href="/finance" class="btn btn-secondary">
        <i class="bi bi-arrow-left"></i> กลับไปหน้ารายงานงบการเงิน 
    </a>
</div>
<%- include('./partials/footer') %>
