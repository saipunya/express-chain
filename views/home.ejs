<%- include('partials/header', { title: 'หน้าแรก - CoopChain' }) %>

<!-- เา่ม FontAwesome ไอคอนดาวน์โหลด -->
<link
  href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
  rel="stylesheet"
/>

<style>
  /* ส่วนล็อก */
  .coop-card {
    border-radius: 1rem;
    overflow: hidden;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12);
    transition: box-shadow 0.3s ease;
  }
  .coop-card:hover {
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.18);
  }
  .coop-card-img {
    height: 100%;
    width: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }
  .coop-card-img:hover {
    transform: scale(1.05);
  }
  .coop-login {
    padding: 2rem;
    background-color: #fff;
    border-radius: 0 1rem 1rem 0;
  }
  @media (max-width: 768px) {
    .coop-card-img {
      height: 250px;
      border-radius: 1rem 1rem 0 0;
    }
    .coop-login {
      border-radius: 0 0 1rem 1rem;
    }
  }

  /* บทความ */
  .card-title {
    font-weight: 600;
  }
  .card-text {
    font-size: 0.9rem;
    color: #555;
  }
  .btn-outline-success:hover {
    background-color: #198754;
    color: white;
  }
  .carousel-item .card {
    margin-bottom: 1.5rem;
  }

  /* ดาวน์โหลด */
  .list-group-item {
    border-radius: 0.5rem;
    transition: background-color 0.3s ease;
  }
  .list-group-item:hover {
    background-color: #e6f4ea;
  }
  .btn-sm {
    padding: 0.375rem 0.75rem;
    font-size: 0.9rem;
  }
  .download-icon {
    margin-right: 8px;
    color: #198754;
  }
</style>

<div class="container my-3">
  <div class="card coop-card">
    <div class="row g-0">
      <!-- ภาพ -->
      <div class="col-md-6">
        <img
          src="https://cdn.pixabay.com/photo/2024/03/26/11/14/ai-generated-8656542_1280.jpg"
          alt="Coop Image"
          class="coop-card-img"
        />
      </div>

      <!-- กล่องล็อก -->
      <div class="col-md-6 d-flex align-items-center">
        <!-- if login show user detail -->
        <% if(!user){ %>
        <div class="coop-login w-100">
          <h1 class="display-6 mb-2 text-success text-center">CoopChainReport</h1>
          <p class="lead mb-4 text-center">ระบบสารสนสารเทศและเครือข่ายสหกรณ์ฯ</p>

          <h5 class="text-center mb-3">🔐 เข้าสู่ระบบ</h5>
          <form action="/auth/login" method="POST">
            <div class="mb-3">
              <label for="username" class="form-label">ชื่อผู้ใช้งาน</label>
              <input
                type="text"
                class="form-control"
                id="username"
                name="username"
                required
              />
            </div>
            <div class="mb-3">
              <label for="password" class="form-label">รหัสผ่าน</label>
              <input
                type="password"
                class="form-control"
                id="password"
                name="password"
                required
              />
            </div>
            <button type="submit" class="btn btn-success w-100">เข้าระบบ</button>
          </form>
          <hr>
          <p class="text-center mb-3"><i class="bi bi-person-add"></i> ยังไม่ได้เป็นสมาชิก <a href="/auth/register">สมัครสมาชิก</a></p>
        </div>
        <% } else { %>
        <div class="coop-login w-100">
          <h1 class="display-6 mb-2 text-success text-center">ยินดีต้อนรับ</h1>
          <p class="lead mb-4 text-center"><%= user.fullname %></p>
          <div class="text-center">
            <a href="/dashboard" class="btn btn-success">แดชบอร์ด</a>
            <a href="/auth/logout" class="btn btn-outline-secondary">ออกจากระบบ</a>
          </div>
        </div>
        <% } %>
      </div>
    </div>
  </div>
</div>

<!-- ส่วนแสดงไฟล์<|im_start|> -->
<section class="py-5 bg-white">
  <div class="container-lg">
    <h2 class="mb-4 text-center text-success">📂 รายงานงบการเงิน</h2>
    
    <div class="bg-light p-4 rounded shadow-sm">
      <ul class="list-group list-group-flush" style="max-width: 900px; margin: auto;">
        <% fileAll.forEach((file, index) => { %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <span>
              <i class="fas fa-file-alt me-2 text-secondary"></i>
              <strong><%= index + 1 %>.</strong> <%= file.c_name %> <span>[ <%= file.end_date %> <%= file.end_year %>]</span>
            </span>
            <% if(user){ %>
              <a 
                href="/download/<%= file.id %>" 
                class="btn btn-sm btn-outline-success" 
                target="_blank" 
                rel="noopener noreferrer"
              >
                ดาวน์โหลด
              </a>
            <% } %>
          </li>
        <% }); %>
      </ul>
    </div>

    <div class="text-center mt-2">
      <a href="/finance" class="btn btn-success btn-lg px-4 shadow-sm">
        📂 งบการเงินทั้งหมด
      </a>
    </div>
  </div>
</section>

<!-- ส่วนแสดงไฟล์ข้อ3 -->
<section class="py-5" style="background-color: #f8f9fa;">
  <div class="container-lg">
    <h2 class="mb-4 text-center text-success">📋 ข้อบังคับสหกรณ์/กลุ่มเกษตรกร</h2>
    
    <div class="bg-white p-4 rounded shadow-sm">
      <ul class="list-group list-group-flush" style="max-width: 900px; margin: auto;">
        <% ruleFiles.forEach((rule, index) => { %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <span>
              <i class="fas fa-file-pdf me-2 text-secondary"></i>
              <strong><%= index + 1 %>.</strong> <%= rule.rule_name %> 
              <span>[ <%= rule.rule_year %>]</span>
              <span class="badge bg-info ms-2">
                <% if (rule.rule_type === 'MR') { %>
                  ข้อบังคับหลัก
                <% } else if (rule.rule_type === 'ER') { %>
                  แก้ไขเพิ่มเติม <%= rule.er_no %>
                <% } %>
              </span>
            </span>
            <% if(user){ %>
              <a 
                href="/rule/<%= rule.rule_id %>" 
                class="btn btn-sm btn-outline-success" 
                target="_blank" 
                rel="noopener noreferrer"
              >
                ดาวน์โหลด
              </a>
            <% } %>
          </li>
        <% }); %>
      </ul>
    </div>

    <div class="text-center mt-2">
      <a href="/rule" class="btn btn-success btn-lg px-4 shadow-sm">
        📋 ข้อบังคับทั้งหมด
      </a>
    </div>
  </div>
</section>

<%- include('partials/footer') %>
