<%- include('../partials/header.ejs') %>
<style>
  .article-card { transition: .25s ease; }
  .article-card:hover { transform: translateY(-4px); box-shadow: 0 14px 30px -10px rgba(0,0,0,.2)!important; }
  .line-clamp-2 {
    display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
  }
  .gradient-bar {
    position:absolute; top:0; left:0; height:4px; width:100%;
    background: linear-gradient(90deg,#0ea5e9,#6366f1,#8b5cf6);
    border-top-left-radius:.5rem; border-top-right-radius:.5rem;
  }
</style>

<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2 class="mb-0">📚 รายการบทความ</h2>
    <a href="/article" class="btn btn-sm btn-outline-primary">รีเฟรช</a>
  </div>
  <p class="text-muted mb-3">อัปเดตล่าสุด • เรียงลำดับตามวันที่บันทึก</p>

  <!-- ใช้ partial รวมรายการบทความ -->
  <%- include('./partials/list-body.ejs', { articles }) %>
</div>
<%- include('../partials/footer.ejs') %>