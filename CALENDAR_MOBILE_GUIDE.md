# 📅 ปฏิทินกิจกรรม (Mobile Calendar)

## รายละเอียด
- **URL**: `/gitgum/mobile`
- **ชนิด**: Standalone PWA (Progressive Web App)
- **ลักษณะ**: ไม่มี header/footer - สำหรับสร้าง shortcut บนมือถือ

## ฟีเจอร์
✅ แสดงกิจกรรมตั้งแต่วันปัจจุบันไปจนถึงวันสุดท้ายที่มีข้อมูล
✅ จัดกลุ่มตามเดือน พร้อมเอกสารย่อ
✅ คลิกดูรายละเอียดใน popup modal
✅ นำทางไปยังเดือนต่าง ๆ ได้อย่างรวดเร็ว
✅ Pull-to-refresh เพื่ออัปเดตข้อมูล
✅ Service Worker สำหรับการทำงาน offline (ทำงานแสดงข้อมูลล่าสุด)
✅ Apple shortcut compatible - สามารถเพิ่มไปที่โฮมสกรีนได้

## การใช้งาน

### 1. เปิดหน้าปฏิทิน
```
https://your-domain.com/gitgum/mobile
```

### 2. สร้าง Shortcut บน iPhone/iPad
- เปิด Safari
- ไปที่ `/gitgum/mobile`
- กดปุ่มแชร์ (Share) → "Add to Home Screen"
- ตั้งชื่อแอป (เช่น "ปฏิทินงาน") และกด "Add"

### 3. สร้าง Shortcut บน Android
- เปิด Chrome
- ไปที่ `/gitgum/mobile`
- กดปุ่ม ⋮ (Menu) → "Install app" หรือ "Add to Home Screen"

## โครงสร้างข้อมูล

### Database (tbl_gitgum)
```
- git_id: ID กิจกรรม
- git_date: วันที่ (YYYY-MM-DD)
- git_time: เวลา (HH:mm หรือ HHMM)
- git_act: ชื่อกิจกรรม
- git_place: สถานที่
- git_respon: ผู้รับผิดชอบ
- git_goto: ผู้เข้าร่วม/เป้าหมาย
- git_group: กลุ่มงาน
- git_maihed: หมายเหตุ
```

## Routes ที่เพิ่มเข้ามา

```javascript
GET /gitgum/mobile        // หน้าปฏิทินแบบ standalone
GET /gitgum/calendar      // ปฏิทินแบบเต็มหน้า (มีปุ่มเพิ่มเติม)
```

## ไฟล์ที่สร้าง/แก้ไข

### Backend
- `models/gitgumModel.js` - เพิ่ม `findFromTodayToEnd()` function
- `controllers/gitgumController.js` - เพิ่ม `mobileCalendarView()` function
- `routes/gitgumRoutes.js` - เพิ่ม route `/gitgum/mobile`

### Frontend
- `views/gitgum_mobile_calendar.ejs` - หน้าปฏิทินแบบ PWA
- `public/sw-calendar.js` - Service Worker สำหรับ offline support
- `public/manifest-calendar.json` - PWA manifest file
- `public/icon/calendar-icon-192.svg` - ไอคอนขนาด 192x192
- `public/icon/calendar-icon-512.svg` - ไอคอนขนาด 512x512

## ส่วน Service Worker

ทำงาน Network-first strategy:
1. พยายาม fetch ข้อมูลจากเซิร์ฟเวอร์ก่อน
2. ถ้าสำเร็จ จะบันทึกลง cache
3. ถ้าไม่สำเร็จ (offline) จะใช้ข้อมูลจาก cache

นี่ช่วยให้แอปทำงานได้แม้เมื่อไม่มีสัญญาณอินเทอร์เน็ต

## ตัวอย่างข้อมูล

```json
{
  "groupedByMonth": {
    "2025-01": [
      {
        "id": 1,
        "date": "2025-01-15",
        "time": "09:00",
        "activity": "ประชุมสภาสหกรณ์",
        "place": "ห้องประชุม ชั้น 2",
        "respon": "นายสมใจ ทำเนียบ",
        "goto": "สมาชิกทุกคน",
        "group": "บริหารสหกรณ์",
        "maihed": "สำคัญ"
      }
    ]
  }
}
```

## Troubleshooting

### ไม่เห็นข้อมูลกิจกรรม
- ตรวจสอบว่าฐานข้อมูล `tbl_gitgum` มีข้อมูลหรือไม่
- ตรวจสอบว่าวันที่ในฐานข้อมูลถูกต้องไหม (ต้องมากกว่าหรือเท่ากับวันนี้)

### Service Worker ไม่ทำงาน
- ต้องใช้ HTTPS (หรือ localhost สำหรับการทดสอบ)
- ลบ cache: DevTools → Application → Clear Storage
- Restart browser

### ไม่สามารถสร้าง Shortcut ได้
- ตรวจสอบว่าติดตั้ง manifest อย่างถูกต้อง
- ตรวจสอบเบราว์เซอร์สนับสนุน PWA (Chrome, Edge, Samsung Internet, Safari 16+)
