<%- include('partials/header', { title: 'หน้าแรก - CoopChain' }) %>

<!-- Include Bootstrap 5 -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<div class="container my-5">
  <div class="card shadow-lg rounded overflow-hidden">
    <div class="row g-0">
      <div class="col-md-6">
        <img src="https://cdn.pixabay.com/photo/2024/03/26/11/14/ai-generated-8656542_1280.jpg" alt="Coop Image" class="img-fluid h-100 w-100 object-fit-cover">
      </div>

      <div class="col-md-6 d-flex align-items-center p-4">
        <% if (!user) { %>
          <div class="w-100">
            <h1 class="display-6 mb-3 text-success text-center">CoopChainReport</h1>
            <p class="lead mb-4 text-center">ระบบสารสนเทศและเครือข่ายสหกรณ์สหกรณ์ฯ</p>

            <h5 class="text-center mb-3">🔐 เข้าระบบ</h5>
            <form action="/auth/login" method="POST">
              <div class="mb-3">
                <label for="username" class="form-label">ชื่อผู้ใช้งาน</label>
                <input type="text" class="form-control" id="username" name="username" required />
              </div>
              <div class="mb-3">
                <label for="password" class="form-label">รหัสผ่าน</label>
                <input type="password" class="form-control" id="password" name="password" required />
              </div>
              <button type="submit" class="btn btn-success w-100">เข้าระบบ</button>
            </form>
            <p class="text-center mt-2">
              <i class="bi bi-person-add"></i> ยังไม่มีบัญชีใช่หรือไม่? 
              <a href="/auth/register" class="text-success">
                สมัครสมาชิก
              </a>
            </p>
          </div>
        <% } else { %>
          <div class="w-100 text-center">
            <h1 class="display-6 mb-3 text-success">ยินดีต้อนรับ</h1>
            <img src="<%= user.m_img %>" alt="User Image" class="rounded-circle mb-3" width="100">
            <p class="lead mb-4"><%= user.fullname %></p>
            <a href="/dashboard" class="btn btn-success me-2">แดชบอร์ด</a>
            <a href="/auth/logout" class="btn btn-outline-secondary">ออกจากระบบ</a>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</div>

<section>
  <div class="container-lg">
    <h2 class="my-3 text-center text-primary">📊 ข้อมูลสหกรณ์</h2>
    <div class="row">
      <div class="col-md-4">
        <div class="card shadow-sm border-0">
          <div class="card-body">
            <h5 class="card-title">จำนวนสหกรณ์</h5>
            <p class="card-text display-6 text-success"><%= stats.coop %></p>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card shadow-sm border-0">
          <div class="card-body">
            <h5 class="card-title">จำนวนกลุ่มเกษตรกร</h5>
            <p class="card-text display-6 text-info"><%= stats.farmer %></p>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card shadow-sm border-0">
          <div class="card-body">
            <h5 class="card-title">อยู่ระหว่างชำระบัญชี</h5>
            <p class="card-text display-6 text-warning"><%= stats.closing %></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="py-4 bg-light">
  <div class="container-lg">
    <h2 class="my-3 text-center text-primary">📊 กราฟข้อมูลสหกรณ์</h2>
    <div class="row">
      <div class="col-md-6">
        <div class="card shadow-sm border-0">
          <div class="card-body">
            <h5 class="card-title text-center">แยกตามประเภท</h5>
            <canvas id="coopGroupChart" width="400" height="200"></canvas>
          </div>
        </div>
      </div>
      <div class="col-md-6">
        <div class="card shadow-sm border-0">
          <div class="card-body">
            <h5 class="card-title text-center">แยกตามกลุ่มส่งเสริมสหกรณ์</h5>
            <canvas id="cGroupChart" width="400" height="200"></canvas>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels@2"></script>
<script>
// กราฟแยกตาม coop_group
const coopGroupData = <%- JSON.stringify(coopGroupChart) %>;
const ctx1 = document.getElementById('coopGroupChart').getContext('2d');
new Chart(ctx1, {
  type: 'pie',
  data: {
    labels: coopGroupData.map(item => item.label),
    datasets: [{
      data: coopGroupData.map(item => item.count),
      backgroundColor: ['#FF6384', '#36A2EB', '#FFCE56', '#4BC0C0']
    }]
  },
  options: {
    responsive: true,
    plugins: {
      legend: {
        position: 'bottom'
      },
      datalabels: {
        display: true,
        color: 'white',
        font: {
          weight: 'bold',
          size: 12
        },
        formatter: function(value, context) {
          const label = context.chart.data.labels[context.dataIndex];
          return label + '\n' + value + ' แห่ง';
        }
      }
    }
  },
  plugins: [ChartDataLabels]
});

// กราฟแยกตาม c_group
const cGroupData = <%- JSON.stringify(cGroupChart) %>;
const ctx2 = document.getElementById('cGroupChart').getContext('2d');
new Chart(ctx2, {
  type: 'doughnut',
  data: {
    labels: cGroupData.map(item => item.label),
    datasets: [{
      data: cGroupData.map(item => item.count),
      backgroundColor: ['#FF9F40', '#FF6384', '#4BC0C0', '#9966FF', '#36A2EB']
    }]
  },
  options: {
    responsive: true,
    plugins: {
      legend: {
        position: 'bottom'
      },
      datalabels: {
        display: true,
        color: 'white',
        font: {
          weight: 'bold',
          size: 12
        },
        formatter: function(value, context) {
          const label = context.chart.data.labels[context.dataIndex];
          return label + '\n' + value + ' แห่ง';
        }
      }
    }
  },
  plugins: [ChartDataLabels]
});
</script>

<section class="py-2 bg-white">
  <div class="container-lg">
    <div class="row">
      <!-- 📂 รายงานงบ -->
      <div class="col-md-6 mb-4">
        <div class="bg-light p-4 rounded shadow-sm h-100">
          <h2 class="mb-4 text-center text-success">📂 รายงานงบการเงิน</h2>
          <ul class="list-group list-group-flush">
            <% finances.slice(0, 10).forEach((file, index) => { %>
              <li class="list-group-item d-flex justify-content-between align-items-center">
                <span>
                  <i class="fas fa-file-alt me-2 text-secondary"></i>
                  <strong><%= index + 1 %>.</strong> <%= file.c_name %> 
                  <span>[ <%= file.end_date %> <%= file.end_year %> ]</span>
                  <%
                    const fileSavedDate = new Date(file.savedate);
                    const currentDate = new Date();
                    const diffDays = Math.ceil((currentDate - fileSavedDate) / (1000 * 60 * 60 * 24));
                  %>
                  <% if (diffDays <= 7) { %>
                    <img src="https://websitearchive2020.nepa.gov.jm/new/images/gif/new-star.gif" alt="New" class="ms-2" style="width: 40px; height: 35px;" />
                  <% } %>
                </span>
                <% if(user){ %>
                  <a 
                    href="/finance/download/<%= file.id %>" 
                    class="btn btn-sm btn-outline-success" 
                    target="_blank" 
                    rel="noopener noreferrer"
                  >
                    ดาวน์โหลด
                  </a>
                <% } %>
              </li>
            <% }); %>
          </ul>
          <div class="text-end mt-2">
            <a href="/finance" class="btn btn-danger btn-sm px-4 shadow-sm">
              📂 ดูทั้งหมด
            </a>
          </div>
        </div>
      </div>

      <!-- 📋 ข้อกำหนดสหกรณ์ -->
      <div class="col-md-6 mb-4">
        <div class="bg-light p-4 rounded shadow-sm h-100">
          <h2 class="mb-4 text-center text-success">📋 ข้อบังคับสหกรณ์/กลุ่มเกษตรกร</h2>
          <ul class="list-group list-group-flush">
            <% ruleFiles.slice(0, 10).forEach((rule, index) => { %>
              <li class="list-group-item d-flex justify-content-between align-items-center">
                <span>
                  <i class="fas fa-file-pdf me-2 text-secondary"></i>
                  <strong><%= index + 1 %>.</strong> <%= rule.rule_name %> 
                  <span class="badge ms-2 <% if (rule.rule_type === 'MR') { %>bg-dark<% } else { %>bg-primary<% } %>">
                    <% if (rule.rule_type === 'MR') { %>
                      ข้อกำหนด พ.ศ.<%= rule.rule_year %>
                    <% } else if (rule.rule_type === 'ER') { %>
                      แก้ไข <%= rule.er_no %> [พ.ศ. <%= rule.rule_year %>]
                    <% } %>
                  </span>
                  <%
                    const ruleSavedDate = new Date(rule.rule_savedate);
                    const currentDate = new Date();
                    const diffDays = Math.ceil((currentDate - ruleSavedDate) / (1000 * 60 * 60 * 24));
                  %>
                  <% if (diffDays <= 7) { %>
                    <img src="https://websitearchive2020.nepa.gov.jm/new/images/gif/new-star.gif" alt="New" class="ms-2" style="width: 45px; height: 32px;" />
                  <% } %>
                </span>
                <% if(user){ %>
                  <a 
                    href="/rule/<%= rule.rule_id %>" 
                    class="btn btn-sm btn-outline-success" 
                    target="_blank" 
                    rel="noopener noreferrer"
                  >
                    ดาวน์โหลด
                  </a>
                <% } %>
              </li>
            <% }); %>
          </ul>
          <div class="text-end mt-2">
            <a href="/rule" class="btn btn-danger btn-sm px-4 shadow-sm">
              📋 ดูทั้งหมด
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="py-2 bg-white">
  <div class="container-lg">
    <div class="bg-light p-4 rounded shadow-sm">
      <h2 class="mb-4 text-center text-warning">📄 ระเบียบสหกรณ์/กลุ่มเกษตรกร</h2>
      <ul class="list-group list-group-flush" style="max-width: 900px; margin: auto;">
        <% rabiabFiles.forEach((rabiab, index) => { %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <span>
              <i class="fas fa-file-alt me-2 text-secondary"></i>
              <strong><%= index + 1 %>.</strong> <%= rabiab.ra_name %> 
              <% 
                const rabiabSavedDate = new Date(rabiab.ra_savedate);
                const currentDate = new Date();
                const diffDays = Math.ceil((currentDate - rabiabSavedDate) / (1000 * 60 * 60 * 24));
              %>
              <% if (diffDays <= 7) { %>
                <img src="https://websitearchive2020.nepa.gov.jm/new/images/gif/new-star.gif" alt="New" class="ms-2" style="width: 45px; height: 32px;" />
              <% } %>
            </span>
            <% if(user){ %>
              <a 
                href="/rabiab/download/<%= rabiab.ra_id %>" 
                class="btn btn-sm btn-outline-warning" 
                target="_blank" 
                rel="noopener noreferrer"
              >
                ดาวน์โหลด
              </a>
            <% } %>
          </li>
        <% }); %>
        <div class="text-end mt-2">
          <a href="/rabiab" class="btn btn-warning btn-sm px-4 shadow-sm">
            📄 ดูทั้งหมด
          </a>
        </div>
      </ul>
    </div>
  </div>
</section>

<section class="py-4 bg-light">
  <div class="container-lg">
    <h2 class="mb-4 text-center text-primary">🚗 รายการใช้รถ</h2>
    
    <div class="card shadow-sm border-0">
      <div class="card-body">
        <ul class="list-group list-group-flush">
          <% usecars.forEach((usecar, index) => { %>
            <li class="list-group-item d-flex justify-content-between align-items-start">
              <div class="ms-2 me-auto">
                <!-- Format Thai Date -->
                <% const date = new Date(usecar.date_car); %>
                <% const options = { year: 'numeric', month: 'long', day: 'numeric' }; %>
                <% const thaiDate = date.toLocaleDateString('th-TH', options); %>
                
                <div class="fw-bold text-success">
                  <%= thaiDate %> || <%= usecar.time_car %>
                  <% 
                    const today = new Date();
                    const isToday = today.toLocaleDateString('th-TH') === date.toLocaleDateString('th-TH');
                  %>
                  <% if (isToday) { %>
                    <img src="https://websitearchive2020.nepa.gov.jm/new/images/gif/new-star.gif" alt="New" class="ms-2" style="width: 40px; height: 35px;" />
                  <% } %>
                </div>
                <div class="text-dark"><%= usecar.act_car %></div>
                <small class="text-muted">[สถานที่: <%= usecar.place_car %>]</small>
              </div>
              <% if(user && user.mClass === 'admin'){ %>
                <div class="d-flex align-items-center">
                  <a href="/usecar/edit/<%= usecar.id_usecar %>" class="btn btn-warning btn-sm me-2">Edit</a>
                  <form action="/usecar/delete/<%= usecar.id_usecar %>" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                  </form>
                </div>
              <% } %>
            </li>
          <% }); %>
        </ul>
      </div>
    </div>
  </div>
</section>

<section class="py-4 bg-light">
  <div class="container-lg">
    <div class="row">
      <!-- ข้อมูลสหกรณ์ -->
      <div class="col-md-4">
        <div class="card shadow-sm border-0 h-100">
          <div class="card-body text-center">
            <h5 class="card-title">📈 ข้อมูลการใช้งาน</h5>
            <div class="mb-3">
              <div class="display-4 text-success"><%= onlineCount %></div>
              <small class="text-muted">ผู้ใช้ออนไลน์</small>
            </div>
            <hr>
            <div class="row text-center">
              <h4 class="col-12 text-center">จำนวน</h4>
              <div class="col-md-4">
                <div class="h4 text-primary"><%= stats.coop  %></div>
                <small>สหกรณ์</small>
              </div>
              <div class="col-md-4">
                <div class="h4 text-success"><%=  stats.farmer %></div>
                <small>กลุ่มเกษตรกร</small>
              </div>
              <div class="col-md-4">
                <div class="h4 text-warning"><%= stats.closing %></div>
                <small>ชำระบัญชี</small>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="col-md-8 mt-4 mt-md-0">
        <h2 class="mb-4 text-center text-success">🟢 Users Online</h2>
        
        <div class="card shadow-sm border-0">
          <div class="card-body">
            <div class="row">
              <% onlineUsers.forEach((user, index) => { %>
                <div class="col-md-6 mb-3">
                  <div class="d-flex align-items-center">
                    <img 
                      src="<%= user.m_img || '/images/default-avatar.png' %>" 
                      alt="Avatar" 
                      class="rounded-circle me-3" 
                      style="width: 40px; height: 40px; object-fit: cover;"
                    >
                    <div>
                      <div class="fw-bold text-success"><%= user.on_name %></div>
                      <small class="text-muted"><%= user.m_position || '32ใช้' %></small>
                      <div class="badge bg-success ms-2">
                        <i class="fas fa-circle" style="font-size: 8px;"></i> ออนไลน์
                      </div>
                    </div>
                  </div>
                </div>
              <% }); %>
            </div>
            
            <% if (onlineUsers.length === 0) { %>
              <div class="text-center text-muted">
                <i class="fas fa-users fa-3x mb-3"></i>
                <p>ไม่มีผู้ใช้งานออนไลน์ในขณะนี้</p>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<%- include('partials/footer') %>
