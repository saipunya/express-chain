<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>แก้ไขการจองห้องประชุม</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container my-5">
    <h1 class="mb-4">แก้ไขการจองห้องประชุม</h1>

    <form action="/meetingroom/edit/<%= meeting.mee_id %>" method="POST" class="row g-3">
      <div class="col-md-4">
        <label for="mee_date" class="form-label">วันที่ประชุม</label>
        <input type="date" id="mee_date" name="mee_date" class="form-control" value="<%= meeting.mee_date ? new Date(meeting.mee_date).toISOString().slice(0,10) : '' %>" required>
      </div>
      <div class="col-md-4">
        <label for="mee_time" class="form-label">เวลา</label>
        <input type="text" id="mee_time" name="mee_time" class="form-control" value="<%= meeting.mee_time %>" required>
      </div>
      <div class="col-md-4">
        <label for="mee_room" class="form-label">ห้องประชุม</label>
        <input type="text" id="mee_room" name="mee_room" class="form-control" value="<%= meeting.mee_room %>" required>
      </div>

      <div class="col-12">
        <label for="mee_subject" class="form-label">เรื่อง/วัตถุประสงค์</label>
        <textarea id="mee_subject" name="mee_subject" class="form-control" rows="3" required><%= meeting.mee_subject %></textarea>
      </div>

      <div class="col-md-4">
        <label for="mee_respon" class="form-label">ผู้รับผิดชอบ</label>
        <input type="text" id="mee_respon" name="mee_respon" class="form-control" value="<%= meeting.mee_respon %>" required>
      </div>
      <div class="col-md-4">
        <label for="mee_saveby" class="form-label">ผู้บันทึก</label>
        <input type="text" id="mee_saveby" name="mee_saveby" class="form-control" value="<%= meeting.mee_saveby %>" required>
      </div>
      <div class="col-md-4">
        <label for="mee_savedate" class="form-label">วันที่บันทึก</label>
        <input type="date" id="mee_savedate" name="mee_savedate" class="form-control" value="<%= meeting.mee_savedate ? new Date(meeting.mee_savedate).toISOString().slice(0,10) : '' %>" required>
      </div>

      <div class="col-12 d-flex gap-2">
        <button class="btn btn-primary" type="submit">บันทึกการแก้ไข</button>
        <a href="/meetingroom" class="btn btn-secondary">ยกเลิก</a>
      </div>
    </form>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
