<%- include('../partials/header') %>

<div class="py-4">
  <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4">
    <div>
      <h1 class="h4 mb-1"><i class="bi bi-bar-chart-steps me-2"></i>ผลจัดชั้นความเข้มแข็งสหกรณ์และกลุ่มเกษตรกร</h1>
    </div>
    <div class="d-flex gap-2 flex-wrap">
      <a href="/newstrength/create" class="btn btn-primary"><i class="bi bi-plus-circle me-1"></i>เพิ่มรายการ</a>
    </div>
  </div>

  <% if (summary && summary.length) { %>
    <div class="card shadow-sm border-0 mb-4">
      <div class="card-header bg-white">
        <h5 class="mb-0"><i class="bi bi-pie-chart text-success me-2"></i>สรุปผลจัดชั้นแยกตามประเภทสถาบัน</h5>
      </div>
      <div class="table-responsive mb-0">
        <table class="table align-middle mb-0">
          <thead class="table-light">
            <tr>
              <th>ประเภทสถาบัน</th>
              <th class="text-end text-success">ชั้น 1</th>
              <th class="text-end text-warning">ชั้น 2</th>
              <th class="text-end text-danger">ชั้น 3</th>
              <th class="text-end fw-bold">รวมทั้งหมด</th>
            </tr>
          </thead>
          <tbody>
            <% 
              let totalG1 = 0, totalG2 = 0, totalG3 = 0, grandTotal = 0;
              summary.forEach(row => { 
                totalG1 += row.counts['ชั้น1'];
                totalG2 += row.counts['ชั้น2'];
                totalG3 += row.counts['ชั้น3'];
                grandTotal += row.total;
            %>
              <tr>
                <td class="fw-medium"><%= row.typeLabel %></td>
                <td class="text-end"><%= row.counts['ชั้น1'] > 0 ? row.counts['ชั้น1'].toLocaleString() : '-' %></td>
                <td class="text-end"><%= row.counts['ชั้น2'] > 0 ? row.counts['ชั้น2'].toLocaleString() : '-' %></td>
                <td class="text-end"><%= row.counts['ชั้น3'] > 0 ? row.counts['ชั้น3'].toLocaleString() : '-' %></td>
                <td class="text-end fw-bold"><%= row.total > 0 ? row.total.toLocaleString() : '-' %></td>
              </tr>
            <% }) %>
          </tbody>
          <tfoot class="table-light">
            <tr>
              <th class="text-end">รวมทุกประเภท</th>
              <th class="text-end text-success"><%= totalG1 > 0 ? totalG1.toLocaleString() : '-' %></th>
              <th class="text-end text-warning"><%= totalG2 > 0 ? totalG2.toLocaleString() : '-' %></th>
              <th class="text-end text-danger"><%= totalG3 > 0 ? totalG3.toLocaleString() : '-' %></th>
              <th class="text-end fw-bold"><%= grandTotal > 0 ? grandTotal.toLocaleString() : '-' %></th>
            </tr>
          </tfoot>
        </table>
      </div>
    </div>
  <% } %>

  <% if (!items || !items.length) { %>
    <div class="text-center text-muted py-5">
      <i class="bi bi-inbox fs-1 d-block mb-3"></i>
      <div>ยังไม่มีข้อมูล</div>
    </div>
  <% } else { %>
    <div class="card shadow-sm border-0">
      <div class="table-responsive mb-0">
        <table class="table align-middle mb-0">
          <thead class="table-light">
            <tr>
              <th style="width:70px;">#</th>
              <th>รหัส (str_code)</th>
              <th>กลุ่ม (str_group)</th>
              <th>ผลจัดชั้น (str_grade)</th>
              <th>หน่วยงาน</th>
              <th style="width:160px;" class="text-center">จัดการ</th>
            </tr>
          </thead>
          <tbody>
            <% items.forEach((row, idx) => { %>
              <tr>
                <td class="text-muted"><%= idx + 1 %></td>
                <td><span class="badge bg-info text-dark"><%= row.str_code %></span></td>
                <td><%= row.str_group %></td>
                <td><span class="badge <%= row.str_grade ? 'bg-success' : 'bg-secondary' %>"><%= row.str_grade || '-' %></span></td>
                <td class="text-muted"><%= row.c_name || '-' %></td>
                <td class="text-center">
                  <div class="btn-group btn-group-sm" role="group">
                    <a class="btn btn-outline-primary" href="/newstrength/<%= row.str_id %>/edit"><i class="bi bi-pencil-square"></i></a>
                    <form method="POST" action="/newstrength/<%= row.str_id %>/delete" onsubmit="return confirm('ยืนยันลบรายการนี้?');" class="d-inline">
                      <button type="submit" class="btn btn-outline-danger"><i class="bi bi-trash"></i></button>
                    </form>
                  </div>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  <% } %>
</div>

<%- include('../partials/footer') %>
