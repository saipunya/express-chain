<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <title>รายการบัญชี</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        @import url("https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css");
    </style>
</head>
<body class="bg-light">

<div class="container py-4">
    <h4 class="mb-4 text-center">📒 รายการบัญชีรายรับรายจ่าย กลุ่มจัดตั้งและส่งเสริมสหกรณ์</h4>

    <% if (user && (user.mClass === 'kts' || user.mClass === 'admin')) { %>
        <a href="/account/create" class="btn btn-success mb-3">
            <i class="bi bi-plus-circle"></i> เพิ่มรายการ
        </a>
    <% } %>

    <!-- แสดงสรุปยอด -->
    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card text-bg-success">
                <div class="card-body">
                    <h5 class="card-title">รวมรายได้</h5>
                    <p class="card-text fs-4"><%= totalIncome.toLocaleString() %> บาท</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card text-bg-danger">
                <div class="card-body">
                    <h5 class="card-title">รวมค่าใช้จ่าย</h5>
                    <p class="card-text fs-4"><%= totalExpense.toLocaleString() %> บาท</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card text-bg-primary">
                <div class="card-body">
                    <h5 class="card-title">ยอดคงเหลือ</h5>
                    <p class="card-text fs-4"><%= balance.toLocaleString() %> บาท</p>
                    <% const thaiDate = new Date().toLocaleDateString('th-TH', { year: 'numeric', month: 'long', day: 'numeric' }); %>
                    <small class="text-white">ณ วันที่ <%= thaiDate %></small>
                </div>
            </div>
        </div>
    </div>

    <!-- ตารางรายการ -->
    <table class="table table-striped table-bordered bg-white">
        <thead class="table-dark">
            <tr>
                <th>วันที่</th>
                <th>รายละเอียด</th>
                <th class="text-end">จำนวนเงิน</th>
                <% if (user && (user.mClass === 'kts' || user.mClass === 'admin')) { %>
                    <th class="text-center">การจัดการ</th>
                <% } %>
            </tr>
        </thead>
        <tbody>
            <% accounts.forEach(account => { %>
                <tr>
                    <!-- วันที่ -->
                    <% const thaiDateItem = new Date(account.date).toLocaleDateString('th-TH', { year: 'numeric', month: 'long', day: 'numeric' }); %>
                    <td><%= thaiDateItem %></td>
                    
                    <!-- รายละเอียด + ประเภท -->
                    <td>
                        <%= account.description %>
                        <% if (account.type === 'income') { %>
                            <span class="badge bg-success">รายได้</span>
                        <% } else { %>
                            <span class="badge bg-danger">ค่าใช้จ่าย</span>
                        <% } %>
                    </td>
                   
                    <!-- จำนวนเงิน -->
                    <% const formattedAmount = account.amount.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ","); %>
                    <td class="text-end"><%= formattedAmount %></td>

                    <!-- ปุ่มแก้ไข/ลบ -->
                    <% if (user && (user.mClass === 'kts' || user.mClass === 'admin')) { %>
                        <td class="text-center">
                            <a href="/account/edit/<%= account.id %>" class="btn btn-warning btn-sm">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                            <a href="/account/delete/<%= account.id %>" class="btn btn-danger btn-sm" onclick="return confirm('คุณต้องการลบรายการนี้หรือไม่?');">
                                <i class="bi bi-trash"></i>
                            </a>
                        </td>
                    <% } %>
                </tr>
            <% }) %>
        </tbody>
    </table>

    <div class="text-center">
        <a href="/" class="btn btn-danger mb-3">
            <i class="bi bi-arrow-left"></i> กลับหน้าหลัก
        </a>
    </div>
</div>

</body>
</html>
