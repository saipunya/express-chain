<!-- list of gitgum find all -->
 <%- include('partials/header') %>

 <div class="container mt-4">
    <h3 class="mb-4">📋 รายการกิจกรรม</h3> 
    <!-- add button -->
     <div class="mb-3 text-end">
      <% if(user && user.mClass=='kjs'){ %>
        <a href="/gitgum/add" class="btn btn-success mb-3">
          <i class="bi bi-plus-circle"></i> เพิ่มรายการ
          </a>
        <% } %>
        <a href="/dashboard/" class="btn btn-primary mb-3">
          <i class="bi bi-house"></i> กลับไปที่แดชบอร์ด
        </a>
     </div>
   
     
      <!-- รับข้อมูลจาก db มาแสดง -->
       <table class="table table-bordered table-striped">
        <thead>
          <tr>
            <th>วันที่</th>
            <th>เวลา</th>
            <th>กิจกรรม</th>
            <th>สถานที่</th>
            <th>ผู้รับผิดชอบ</th>
            <% if(user && user.mClass=='kjs'){ %>
            <th>Manage</th>
            <% } %>
          </tr>
        </thead>
        <tbody>
          <% data.forEach(gitgum => { %>
            <tr>
              <!-- thai date -->
               <%
                 let thaiDate = '-';
                 if (gitgum.git_date) {
                   const date = gitgum.git_date instanceof Date ? gitgum.git_date : new Date(gitgum.git_date);
                   if (!Number.isNaN(date.getTime())) {
                     thaiDate = date.toLocaleDateString('th-TH', { year: 'numeric', month: 'long', day: 'numeric', timeZone: 'Asia/Bangkok' });
                   } else {
                     thaiDate = String(gitgum.git_date).slice(0, 10);
                   }
                 }
               %>
               <td><%= thaiDate %></td>
              <td><%= gitgum.git_time %></td>
              <td><%= gitgum.git_act %></td>
              <td><%= gitgum.git_place %></td>
              <td><%= gitgum.git_goto %></td>
            
              <% if(user && user.mClass=='kjs'){ %>
              <td class="text-center text-nowrap">
                <a href="/gitgum/view/<%= gitgum.git_id %>" class="btn btn-info btn-sm">View</a>
                <a href="/gitgum/edit/<%= gitgum.git_id %>" class="btn btn-warning btn-sm">Edit</a>
                <a href="/gitgum/delete/<%= gitgum.git_id %>" class="btn btn-danger btn-sm" onclick="return confirm('ลบข้อมูล?')">Delete</a>
              </td>
              <% } %>
            </tr>
          <% }); %>
          </tbody>
       </table>
       <!-- pagination controls -->
       <% if (pagination && pagination.totalPages > 1) { %>
        <nav aria-label="Gitgum pagination">
          <ul class="pagination justify-content-center">
            <li class="page-item <%= !pagination.hasPrev ? 'disabled' : '' %>">
              <a class="page-link" href="/gitgum/list?page=<%= pagination.page - 1 %>&pageSize=<%= pagination.pageSize %>">ก่อนหน้า</a>
            </li>

            <% for (let p = 1; p <= pagination.totalPages; p++) { %>
              <li class="page-item <%= p === pagination.page ? 'active' : '' %>">
                <a class="page-link" href="/gitgum/list?page=<%= p %>&pageSize=<%= pagination.pageSize %>"><%= p %></a>
              </li>
            <% } %>

            <li class="page-item <%= !pagination.hasNext ? 'disabled' : '' %>">
              <a class="page-link" href="/gitgum/list?page=<%= pagination.page + 1 %>&pageSize=<%= pagination.pageSize %>">ถัดไป</a>
            </li>
          </ul>
        </nav>
       <% } %>


    </div>
    



 <%- include('partials/footer') %>